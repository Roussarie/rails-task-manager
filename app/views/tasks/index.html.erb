<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task) %>
    </li>
     <%= link_to task_path(task), method: :delete, data:{ confirm: "Delete this task?"} do %><i class="fas fa-trash-alt"></i>
  <%end%>
  <% end %>
  <%= link_to "Add a new task", new_task_path %>
</ul>
